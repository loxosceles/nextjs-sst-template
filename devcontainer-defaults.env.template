# devcontainer-defaults.env
# Copy to ~/.config/devcontainer-defaults.env and fill in your values.
# setup.sh sources this file before prompting — any value set here becomes the default answer.

GITHUB_USERNAME=
GIT_NAME=
GIT_EMAIL=
SSH_MODE=            # "contexts" or "default"
SSH_CONTEXT=         # only used when SSH_MODE=contexts
AWS_CREDENTIALS_PATH=
ZSH_CONFIG_PATH=
TMUX_CONFIG_PATH=
ZSH_HISTORY_PATH=
ZSH_HISTORY_TMUX_PATH=
CODING_STANDARDS_PATH=
