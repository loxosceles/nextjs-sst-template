# .devcontainer/.env — local devcontainer environment variables
# Copy this file to .devcontainer/.env and fill in your values.
# This file is gitignored — never commit real values.

# GitHub username (used by post_create.sh for dotfiles)
GITHUB_USERNAME=

# Git identity
GIT_NAME=
GIT_EMAIL=
